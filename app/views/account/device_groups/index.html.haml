.container.col-md-6
  %h1 Device groups
  %table.text-left
    %thead
      %tr
        %th Name
    %tbody
      = @device_group.each do |device_group|
        %tr
          %td
            = link_to  device_group.name, account_device_groups_path(device_group)
          %td
            = link_to "Edit", edit_account_device_groups_path(device_group)
          %td
            = link_to "Delete", account_device_groups_path(device_group), method: :delete, data: {confirm: "Are you sure?"}